function drawRectangle(option) {
    var width = option.width;
    var length = option.length;
    if (option.height) {
        var height = option.height;
        //draw 3D logic
    }
    //draw 2D logic
}
drawRectangle({
    width: 100,
    length: 200
});
